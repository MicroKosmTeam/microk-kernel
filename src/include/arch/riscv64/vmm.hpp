#pragma once
#include <memory.hpp>

namespace RISCV64 {

#define VMM_FLAGS_READ       (0)
#define VMM_FLAGS_NOEXEC     (0)
#define VMM_FLAGS_WRITE      (0)
#define VMM_FLAGS_NOCACHE    (0)
#define VMM_FLAGS_USER       (0)
#define VMM_FLAGS_SHAREABLE  (0)
#define VMM_FLAGS_HUGE       (0)

}
